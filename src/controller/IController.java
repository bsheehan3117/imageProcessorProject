package controller;

/**
 * Interface representing a controller for taking user commands.
 */
public interface IController {
  /**
   * Method for starting the controller for taking commands.
   */
  void go();
}
