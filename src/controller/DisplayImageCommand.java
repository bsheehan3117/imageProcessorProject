package controller;

import java.util.Scanner;

import controller.commands.ICommand;
import model.IImageDataBase;

/**
 * Represents a command to display an image.
 */

public class DisplayImageCommand implements ICommand {


  /**
   * Runs the command for displaying an image (Impl next hw).
   *     @param scanner Scans input.
   *     @param model   The image database model.
   */
  @Override
  public void run(Scanner scanner, IImageDataBase model) {
    // Keeping this for next hw.

  }
}
